import React from 'react';
import ReactDOM from 'react-dom';
//the app itself being rendered through the 'root'//
import App from './App';
import './index.css';

ReactDOM.render(<App />, document.getElementById('root'));